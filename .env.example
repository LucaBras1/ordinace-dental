# Comgate Payment Gateway
COMGATE_MERCHANT_ID="your_merchant_id"
COMGATE_SECRET="your_secret_key"
COMGATE_TEST_MODE="true"

# SMTP Configuration (Nodemailer)
# Pro lokalni Postfix na VPS staci:
SMTP_HOST="localhost"
SMTP_PORT="25"
# SMTP_USER a SMTP_PASS nejsou potreba pro localhost

# Pro externi SMTP (Gmail, etc.):
# SMTP_HOST="smtp.gmail.com"
# SMTP_PORT="587"
# SMTP_USER="your@gmail.com"
# SMTP_PASS="app-password"

EMAIL_FROM="Dentalni ordinace <rezervace@dvi12.vas-server.cz>"

# Google Calendar API Integration (REQUIRED)
# This is the single source of truth for all bookings.
#
# Setup guide:
# 1. Create project in Google Cloud Console: https://console.cloud.google.com/
# 2. Enable Google Calendar API: APIs & Services → Library → "Google Calendar API"
# 3. Create OAuth 2.0 credentials: APIs & Services → Credentials → Create Credentials → OAuth client ID
# 4. Get refresh token via OAuth2 flow (use Google OAuth Playground: https://developers.google.com/oauthplayground/)
# 5. Create a dedicated calendar for bookings in Google Calendar
# 6. Copy Calendar ID from calendar settings

GOOGLE_CLIENT_ID="your_client_id.apps.googleusercontent.com"
GOOGLE_CLIENT_SECRET="your_client_secret"
GOOGLE_REFRESH_TOKEN="your_refresh_token"
GOOGLE_CALENDAR_ID="your_calendar_id@group.calendar.google.com"
GOOGLE_REDIRECT_URI="http://localhost:3000/api/auth/google/callback"

# Application
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Contact Information (used in email templates)
CONTACT_PHONE="+420 601 532 676"
CONTACT_EMAIL="info@ordinace.cz"
CONTACT_ADDRESS="Adresa ordinace, Praha"

# Cron Job Authentication (REQUIRED for production)
# Generate with: openssl rand -hex 32
# Used to authenticate calls to /api/cron/* endpoints
CRON_SECRET="your_cron_secret"

# Testing (optional)
# TEST_EMAIL="your-test-email@example.com"
